import re
# <str>   = re.sub(<regex>, new, text, count=0)  # Заменяет все вхождения на «новые».
# <list>  = re.findall(<regex>, text)            # Returns all occurrences as strings.
# <list>  = re.split(<regex>, text, maxsplit=0)  # Возвращает все вхождения в виде списка строк.
# <Match> = re.search(<regex>, text)             # Ищет первое вхождение шаблона.
# <Match> = re.match(<regex>, text)              # Ищет только в начале текста.
# <iter>  = re.finditer(<regex>, text)           # Возвращает все вхождения как объекты соответствия.
